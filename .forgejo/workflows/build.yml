name: Build

on:
  push:

jobs:
  build:
    runs-on: java21
    uses: BloodStone/actions/.forgejo/workflows/build-and-publish.yml@main
    with:
      gradle_tasks: build
      maven_goals: -C package
    secrets: inherit
